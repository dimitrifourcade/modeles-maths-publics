# Import documents validés — v7 chemins courts

Cette archive est faite pour le dépôt `modeles-maths-publics`.

## Où placer les fichiers ?

Dézipper cette archive puis copier son contenu à la racine du dépôt GitHub.

Elle respecte les dossiers existants du dépôt :

- `documents_a_classer/`

Elle ne crée pas de dossier `gh_v6/` ni de dossier `docs/` parallèle.

## Chemins courts

Les noms des PDF ajoutés ont été raccourcis pour éviter les erreurs Windows.
La correspondance avec les noms précédents est dans :

`documents_a_classer/99_exports_index_json/import_v7/MAPPING_CHEMINS_V7.csv`

## Commandes Git

```bash
git add documents_a_classer README_IMPORT_V7.md
git commit -m "Ajout documents valides dans arborescence existante"
git push
```
