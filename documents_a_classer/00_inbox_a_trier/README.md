# Inbox à trier

Dépose ici rapidement les fichiers sans réfléchir.

Conseil :
- PDF dans `pdf/`
- sources `.tex` dans `latex/`
- images dans `images/`

Ensuite, utiliser `/import-rapide` sur le site pour générer un bloc IA ou préparer une entrée `index.json`.
