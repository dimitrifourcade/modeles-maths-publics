# Import GitHub — révisions 1Spé v49

Déposer le contenu de cette archive à la racine du dépôt, au même niveau que `modeles/`, `docs/`, `regles_ia/` et `index.json`.

Dossier ajouté : `modeles/fiches_revision/1spe/rev/`.

L’archive contient aussi un `index.json` complet mis à jour à partir de l’archive de structure fournie. Si ton index en ligne a changé, compare avant d’écraser ; sinon utilise `index_rev1spe_ADD_v49.json` pour copier seulement l’entrée.
